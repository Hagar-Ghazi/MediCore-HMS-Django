[project]
name        = "medicore-hms"
version     = "1.0.0"
description = "MediCore Hospital Management System with Ollama AI Integration"
readme      = "README.md"
requires-python = ">=3.12"

dependencies = [
    "django>=6.0.4",
    "python-dotenv>=1.0.0",
    "requests>=2.32.0",
]
